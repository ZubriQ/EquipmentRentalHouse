<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate TargetType="Button" x:Key="Submit" >
        <Border x:Name="btnSubmit" 
                    BorderThickness="0" Background="#D8D2CB" Height="32">

            <ContentControl Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}" 
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Foreground="White" FontSize="18" FontFamily="Fonts/#Dosis" />
        </Border>
        
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="btnSubmit" Property="Background" Value="#ADA9A3"/>
            </Trigger>

            <Trigger Property="IsPressed" Value="true">
                <Setter TargetName="btnSubmit" Property="Background" Value="#827E7A"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>